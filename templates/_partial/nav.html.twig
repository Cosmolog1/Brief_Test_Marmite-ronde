<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Marmite Ronde</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Home</a>
                </li>
                {% if app.user %}
                <li  class="nav-item">
                    Bienvenue : {{app.user.email}} <a href="{{path('app_logout')}}">Se deconnecter</a>
                </li>
                {% else %}
                <li  class="nav-item">
                    <a href="{{path('app_login')}}">Se connecter </a><a href="{{path('app_register')}}">/ S'enregistrer</a>
                </li>
                {% endif %}

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Menu
                    </a>
                    {% if app.user %} 
                        {% if is_granted("ROLE_ADMIN") %}
                            <ul class="dropdown-menu">
                                <li>
                                    <li><a class="dropdown-item" href="{{ path ('app_admin_ingredient') }}">Ingredient</a></li>
                                    <li><a class="dropdown-item" href="{{ path ('app_admin_difficult') }}">Difficult</a></li>
                                    <li><a class="dropdown-item" href="{{ path ('app_admin_recipe') }}">Recipe</a></li>
                                </li>    
                            </ul>
                        {% endif %}
                        {% if is_granted("ROLE_USERCO") %}
                            <ul class="dropdown-menu">
                                <li>
                                 <li><a class="dropdown-item" href="{{ path ('app_userco_recipe') }}">Recipe</a></li>
                                 </li>    
                            </ul>
                        {% endif %}
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>